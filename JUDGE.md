# 90-second judge script

Record against the **public Vercel URL**, not localhost. Masthead links are the chapters. The page shows the Celo Sepolia rehearsal. That is the entry.

Mainnet identity is agent [9807](https://8004scan.io/agents/celo/9807). Do not apologise for Sepolia. The escrow was executed on a Celo chain, the tag was decoded from calldata, and the Call section is the product.

1. `#call`. Press **Meet the SLA**, then **Miss: HTTP 500**. The log is the product.
2. `#healthy`. Paid call: 21ms, HTTP 200, 0.001 USDC. Settlement tx on Sepolia.
3. `#breach`. HTTP 500. Charged 0. No refund.
4. `#settle`. Attribution decoded: `x402_sla`, `celo_5ffb6e9c75fb`. Close on agent 9807.

Do not add a fifth beat. Do not mention bridging.
